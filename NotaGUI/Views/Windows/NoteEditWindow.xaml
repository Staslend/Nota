<Window x:Class="NotaGUI.Views.Windows.NoteEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NotaGUI.Views.Windows"
        mc:Ignorable="d"
        Title="NoteEditWindow" Height="600" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Button x:Name="SaveNoteTextButton" Content="Save" Click="SaveNoteTextButton_Click"/>
            </Grid>


            <Grid Grid.Column="1">
                <TextBox x:Name="SubjectField" FontSize="25"/>
            </Grid>

        </Grid>
    
        <Grid Grid.Row="1">
            <TextBox x:Name="TextField" FontSize="25" />
        </Grid>

    </Grid>
</Window>
